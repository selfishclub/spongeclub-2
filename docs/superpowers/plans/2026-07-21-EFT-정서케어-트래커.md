# EFT 정서케어 트래커 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 화병 증상 자가진단(0~4)과 EFT 8단계 자가치료(Before/After NRS 0~10)를 기록하고 추이를 보여주는 정적 웹앱 프로토타입을 만든다.

**Architecture:** 빌드 도구 없는 순수 정적 웹앱. 순수 로직(점수·스트릭·저장)은 브라우저와 Node 양쪽에서 돌아가는 ES 모듈로 분리해 `node --test`로 단위 테스트하고, 화면(뷰)은 각 뷰 파일이 하나의 화면을 그리는 vanilla DOM 코드로 구현해 브라우저에서 수동 검증한다. 데이터는 `localStorage`에만 저장한다.

**Tech Stack:** HTML + CSS + Vanilla JS (ES Modules), Node 내장 test runner(`node --test`), 외부 라이브러리·CDN 없음, 그래프는 인라인 SVG.

## Global Constraints

- 빌드 도구·번들러 없음. 순수 HTML/CSS/JS(ES Modules)만 사용한다.
- 외부 CDN·서드파티 라이브러리 의존성 없음. 그래프는 인라인 SVG로 직접 그린다.
- 데이터는 브라우저 `localStorage`에만 저장한다. 서버·DB·로그인·계정 없음.
- UI 문구에 **"마음침"이라는 단어를 절대 쓰지 않는다.** 브랜딩은 **"EFT"** 로만 한다.
- Beck 불안척도(BAI)·Beck 우울척도(BDI) 문항을 넣지 않는다.
- 화병 증상 15문항은 아래 명시된 문구를 그대로 쓰고, 앱 안에 출처("문항 출처: 권정혜·김종우, 화병척도")를 표기한다.
- 일일 자가진단 척도는 0~4점. EFT 세션 감정 강도는 NRS 0~10.
- 앱 루트 폴더: `1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/` (이하 `<APP>`)
- localStorage 최상위 키는 `eft_tracker_v1` 하나만 쓴다.

---

### Task 1: 프로젝트 스캐폴드 · 정적 데이터 · 유틸

**Files:**
- Create: `<APP>/package.json`
- Create: `<APP>/js/data.js`
- Create: `<APP>/js/util.js`
- Test: `<APP>/test/util.test.js`

**Interfaces:**
- Produces (`js/data.js`):
  - `SYMPTOM_QUESTIONS: string[]` (15개)
  - `SYMPTOM_SCALE_LABELS: string[]` (5개, index 0~4)
  - `EFT_TAP_POINTS: {name: string, hint: string}[]`
  - `EFT_GUIDE_STEPS: string[]`
  - `SHAPE_FIELDS: {key: string, label: string, placeholder: string}[]` (key: temperature|texture|color|form|size|weight)
  - `STORAGE_KEY: string` = `'eft_tracker_v1'`
- Produces (`js/util.js`):
  - `todayStr(d?: Date): string` — 로컬 기준 `YYYY-MM-DD`
  - `el(tag: string, props?: object, ...children): HTMLElement` — 간이 DOM 빌더

- [ ] **Step 1: package.json 작성**

`<APP>/package.json`:
```json
{
  "name": "eft-tracker",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test"
  }
}
```

- [ ] **Step 2: 정적 데이터 파일 작성**

`<APP>/js/data.js`:
```js
export const STORAGE_KEY = 'eft_tracker_v1';

export const SYMPTOM_QUESTIONS = [
  '내 삶은 불행한 편이다.',
  '한스러워지는 때가 있다.',
  '내 인생이 서글프다고 느낀다.',
  '나는 서러움을 느낀다.',
  '나는 억울함을 느낀다.',
  '나는 신경이 아주 약해져서 마음을 가눌 수 없다.',
  '나는 손발이 떨리고 안절부절 못한다.',
  '나는 내 자신에게 실망할 때가 많다.',
  '얼굴에 열이 자주 달아오른다.',
  '가슴속에 열이 차 있는 것을 자주 느낀다.',
  '무언가가 아래(다리 또는 배)에서 위(가슴)로 치미는 것을 자주 느낀다.',
  '화가 나면 손이 저리거나 떨린다.',
  '소화가 잘 안 되고 체하는 편이다.',
  '몹시 피곤하다.',
  '세상이 불공평하다고 느낀다.',
];

export const SYMPTOM_SCALE_LABELS = [
  '전혀 그렇지 않다',
  '그렇지 않은 편이다',
  '중간 정도 그렇다',
  '상당히 그렇다',
  '완전히 그렇다',
];

export const EFT_TAP_POINTS = [
  { name: '손날점(준비)', hint: '손날 가운데를 두드리며 준비 확언' },
  { name: '정수리', hint: '머리 꼭대기' },
  { name: '눈썹', hint: '눈썹 안쪽 시작점' },
  { name: '눈 옆', hint: '눈 바깥쪽 뼈' },
  { name: '눈 밑', hint: '눈 아래 뼈' },
  { name: '코 밑', hint: '코와 윗입술 사이' },
  { name: '턱', hint: '아랫입술과 턱 사이' },
  { name: '쇄골', hint: '쇄골 아래 오목한 곳' },
  { name: '겨드랑이 아래', hint: '옆구리, 겨드랑이에서 손바닥 하나 아래' },
];

export const EFT_GUIDE_STEPS = [
  'EFT는 침 없이 손끝으로 특정 지점(타점)을 가볍게 두드리며 감정을 다스리는 오픈소스 자가치료법입니다.',
  '검지·중지 두 손가락 끝으로 각 타점을 5~7회 톡톡 두드립니다.',
  '두드리는 동안 다스리려는 감정과 그 느낌에 마음을 집중합니다.',
  '순서: 손날점(준비) → 정수리 → 눈썹 → 눈 옆 → 눈 밑 → 코 밑 → 턱 → 쇄골 → 겨드랑이 아래',
  '한 바퀴 돈 뒤 감정 강도를 다시 확인합니다.',
];

export const SHAPE_FIELDS = [
  { key: 'temperature', label: '온도', placeholder: '예: 뜨겁다 / 차갑다 / 미지근하다' },
  { key: 'texture', label: '촉감·질감', placeholder: '예: 거칠다 / 끈적하다 / 딱딱하다' },
  { key: 'color', label: '색깔', placeholder: '예: 검붉은 / 회색 / 탁한' },
  { key: 'form', label: '모양', placeholder: '예: 뾰족한 덩어리 / 소용돌이' },
  { key: 'size', label: '크기', placeholder: '예: 주먹만 한 / 손바닥만 한' },
  { key: 'weight', label: '무게', placeholder: '예: 돌덩이처럼 무겁다 / 가볍다' },
];
```

- [ ] **Step 3: 유틸 파일 작성**

`<APP>/js/util.js`:
```js
export function todayStr(d = new Date()) {
  const y = d.getFullYear();
  const m = String(d.getMonth() + 1).padStart(2, '0');
  const day = String(d.getDate()).padStart(2, '0');
  return `${y}-${m}-${day}`;
}

export function el(tag, props = {}, ...children) {
  const node = document.createElement(tag);
  for (const [k, v] of Object.entries(props)) {
    if (k === 'class') node.className = v;
    else if (k === 'text') node.textContent = v;
    else if (k === 'html') node.innerHTML = v;
    else if (k.startsWith('on') && typeof v === 'function') {
      node.addEventListener(k.slice(2).toLowerCase(), v);
    } else {
      node.setAttribute(k, v);
    }
  }
  for (const c of children) {
    if (c == null) continue;
    node.appendChild(typeof c === 'string' ? document.createTextNode(c) : c);
  }
  return node;
}
```

- [ ] **Step 4: todayStr 테스트 작성**

`<APP>/test/util.test.js`:
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { todayStr } from '../js/util.js';

test('todayStr formats a fixed date as YYYY-MM-DD', () => {
  const d = new Date(2026, 6, 5); // 2026-07-05 (월은 0-based)
  assert.equal(todayStr(d), '2026-07-05');
});

test('todayStr zero-pads month and day', () => {
  const d = new Date(2026, 0, 9); // 2026-01-09
  assert.equal(todayStr(d), '2026-01-09');
});
```

- [ ] **Step 5: 테스트 실패 확인**

Run: `cd "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker" && node --test`
Expected: `util.js`가 아직 없거나 통과할 수 있으나, 이 시점엔 `js/util.js`가 이미 있으므로 PASS 가능. 핵심은 `node --test`가 test 파일을 찾아 실행되는 것. (util.js를 먼저 안 만든 경우) FAIL: `Cannot find module '../js/util.js'`.

- [ ] **Step 6: 테스트 통과 확인**

Run: `cd "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker" && node --test`
Expected: PASS (2 tests)

- [ ] **Step 7: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/package.json" "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/data.js" "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/util.js" "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/test/util.test.js"
git commit -m "feat(eft): 프로젝트 스캐폴드 + 정적 데이터 + util(todayStr, el)"
```

---

### Task 2: 점수 계산 로직 (scoring.js)

**Files:**
- Create: `<APP>/js/scoring.js`
- Test: `<APP>/test/scoring.test.js`

**Interfaces:**
- Produces:
  - `computeDailyTotal(scores: number[]): number` — 길이 15, 각 0~4 검증 후 합계. 위반 시 throw.
  - `nrsDelta(before: number, after: number): number` — `before - after` (양수 = 호전)

- [ ] **Step 1: 실패 테스트 작성**

`<APP>/test/scoring.test.js`:
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { computeDailyTotal, nrsDelta } from '../js/scoring.js';

test('computeDailyTotal sums 15 scores', () => {
  assert.equal(computeDailyTotal(new Array(15).fill(4)), 60);
});

test('computeDailyTotal of all zeros is 0', () => {
  assert.equal(computeDailyTotal(new Array(15).fill(0)), 0);
});

test('computeDailyTotal throws on wrong length', () => {
  assert.throws(() => computeDailyTotal([1, 2, 3]));
});

test('computeDailyTotal throws on out-of-range value', () => {
  const bad = new Array(15).fill(0);
  bad[0] = 5;
  assert.throws(() => computeDailyTotal(bad));
});

test('nrsDelta returns improvement (before - after)', () => {
  assert.equal(nrsDelta(8, 3), 5);
});

test('nrsDelta is negative when worse', () => {
  assert.equal(nrsDelta(2, 6), -4);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker" && node --test test/scoring.test.js`
Expected: FAIL — `Cannot find module '../js/scoring.js'`

- [ ] **Step 3: 구현 작성**

`<APP>/js/scoring.js`:
```js
export function computeDailyTotal(scores) {
  if (!Array.isArray(scores) || scores.length !== 15) {
    throw new Error('scores must be an array of 15 numbers');
  }
  for (const s of scores) {
    if (typeof s !== 'number' || Number.isNaN(s) || s < 0 || s > 4) {
      throw new Error('each score must be a number between 0 and 4');
    }
  }
  return scores.reduce((a, b) => a + b, 0);
}

export function nrsDelta(before, after) {
  return before - after;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker" && node --test test/scoring.test.js`
Expected: PASS (6 tests)

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/scoring.js" "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/test/scoring.test.js"
git commit -m "feat(eft): 점수 계산 로직(computeDailyTotal, nrsDelta) + 테스트"
```

---

### Task 3: 연속 실천일수 로직 (streak.js)

**Files:**
- Create: `<APP>/js/streak.js`
- Test: `<APP>/test/streak.test.js`

**Interfaces:**
- Produces:
  - `computeStreak(dailyChecks: {date: string}[], today: string): number` — `today`(YYYY-MM-DD)를 포함해 연속으로 기록이 있는 날의 수. 오늘 기록이 없으면 0. 중복 날짜는 1로.

- [ ] **Step 1: 실패 테스트 작성**

`<APP>/test/streak.test.js`:
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { computeStreak } from '../js/streak.js';

test('empty checks => 0', () => {
  assert.equal(computeStreak([], '2026-07-21'), 0);
});

test('today only => 1', () => {
  assert.equal(computeStreak([{ date: '2026-07-21' }], '2026-07-21'), 1);
});

test('today + yesterday => 2', () => {
  assert.equal(computeStreak([{ date: '2026-07-20' }, { date: '2026-07-21' }], '2026-07-21'), 2);
});

test('gap breaks streak', () => {
  const checks = [{ date: '2026-07-18' }, { date: '2026-07-20' }, { date: '2026-07-21' }];
  assert.equal(computeStreak(checks, '2026-07-21'), 2);
});

test('no check today => 0', () => {
  assert.equal(computeStreak([{ date: '2026-07-20' }], '2026-07-21'), 0);
});

test('duplicate dates count once', () => {
  assert.equal(computeStreak([{ date: '2026-07-21' }, { date: '2026-07-21' }], '2026-07-21'), 1);
});

test('crosses month boundary', () => {
  const checks = [{ date: '2026-06-30' }, { date: '2026-07-01' }];
  assert.equal(computeStreak(checks, '2026-07-01'), 2);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker" && node --test test/streak.test.js`
Expected: FAIL — `Cannot find module '../js/streak.js'`

- [ ] **Step 3: 구현 작성**

`<APP>/js/streak.js`:
```js
function previousDay(dateStr) {
  const [y, m, d] = dateStr.split('-').map(Number);
  const dt = new Date(Date.UTC(y, m - 1, d));
  dt.setUTCDate(dt.getUTCDate() - 1);
  const yy = dt.getUTCFullYear();
  const mm = String(dt.getUTCMonth() + 1).padStart(2, '0');
  const dd = String(dt.getUTCDate()).padStart(2, '0');
  return `${yy}-${mm}-${dd}`;
}

export function computeStreak(dailyChecks, today) {
  const dates = new Set(dailyChecks.map((c) => c.date));
  let streak = 0;
  let cursor = today;
  while (dates.has(cursor)) {
    streak += 1;
    cursor = previousDay(cursor);
  }
  return streak;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker" && node --test test/streak.test.js`
Expected: PASS (7 tests)

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/streak.js" "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/test/streak.test.js"
git commit -m "feat(eft): 연속 실천일수(computeStreak) + 테스트"
```

---

### Task 4: 저장소 로직 (storage.js)

**Files:**
- Create: `<APP>/js/storage.js`
- Test: `<APP>/test/storage.test.js`

**Interfaces:**
- Consumes: `STORAGE_KEY` (from `js/data.js`)
- Produces:
  - `defaultState(): State` — `{ onboardingDone:false, eftGuideSeen:false, dailyChecks:[], eftSessions:[] }`
  - `createStore(backend)` — `backend`는 `{getItem(k), setItem(k,v)}` (localStorage 호환). 반환 객체:
    - `load(): State`
    - `save(state): void`
    - `setOnboardingDone(): State`
    - `setEftGuideSeen(): State`
    - `addDailyCheck(check: {date, symptomScores, total}): State`
    - `addSession(session): sessionWithId` — id 없으면 부여 후 반환

- [ ] **Step 1: 실패 테스트 작성**

`<APP>/test/storage.test.js`:
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { createStore, defaultState } from '../js/storage.js';

function memoryBackend() {
  const map = new Map();
  return {
    getItem: (k) => (map.has(k) ? map.get(k) : null),
    setItem: (k, v) => { map.set(k, String(v)); },
  };
}

test('load returns default state when empty', () => {
  const store = createStore(memoryBackend());
  assert.deepEqual(store.load(), defaultState());
});

test('addDailyCheck persists', () => {
  const store = createStore(memoryBackend());
  store.addDailyCheck({ date: '2026-07-21', symptomScores: new Array(15).fill(1), total: 15 });
  const s = store.load();
  assert.equal(s.dailyChecks.length, 1);
  assert.equal(s.dailyChecks[0].total, 15);
});

test('addSession assigns an id and persists', () => {
  const store = createStore(memoryBackend());
  const saved = store.addSession({
    datetime: '2026-07-21T10:00:00Z', situation: 'x', emotions: 'y',
    chosenEmotion: '억울함', before: 8, shape: {}, bodyLocation: '가슴', after: 3,
  });
  assert.ok(saved.id);
  assert.equal(store.load().eftSessions.length, 1);
});

test('setOnboardingDone and setEftGuideSeen flip flags', () => {
  const store = createStore(memoryBackend());
  store.setOnboardingDone();
  store.setEftGuideSeen();
  const s = store.load();
  assert.equal(s.onboardingDone, true);
  assert.equal(s.eftGuideSeen, true);
});

test('load merges missing keys onto malformed/partial data', () => {
  const backend = memoryBackend();
  backend.setItem('eft_tracker_v1', JSON.stringify({ onboardingDone: true }));
  const store = createStore(backend);
  const s = store.load();
  assert.equal(s.onboardingDone, true);
  assert.deepEqual(s.dailyChecks, []);
  assert.deepEqual(s.eftSessions, []);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker" && node --test test/storage.test.js`
Expected: FAIL — `Cannot find module '../js/storage.js'`

- [ ] **Step 3: 구현 작성**

`<APP>/js/storage.js`:
```js
import { STORAGE_KEY } from './data.js';

export function defaultState() {
  return {
    onboardingDone: false,
    eftGuideSeen: false,
    dailyChecks: [],
    eftSessions: [],
  };
}

export function createStore(backend) {
  function load() {
    const raw = backend.getItem(STORAGE_KEY);
    if (!raw) return defaultState();
    try {
      return { ...defaultState(), ...JSON.parse(raw) };
    } catch {
      return defaultState();
    }
  }

  function save(state) {
    backend.setItem(STORAGE_KEY, JSON.stringify(state));
  }

  function update(mutator) {
    const state = load();
    mutator(state);
    save(state);
    return state;
  }

  return {
    load,
    save,
    setOnboardingDone() { return update((s) => { s.onboardingDone = true; }); },
    setEftGuideSeen() { return update((s) => { s.eftGuideSeen = true; }); },
    addDailyCheck(check) { return update((s) => { s.dailyChecks.push(check); }); },
    addSession(session) {
      const withId = {
        ...session,
        id: session.id || `s_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`,
      };
      update((s) => { s.eftSessions.push(withId); });
      return withId;
    },
  };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker" && node --test`
Expected: PASS (전체: util 2 + scoring 6 + streak 7 + storage 5 = 20 tests)

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/storage.js" "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/test/storage.test.js"
git commit -m "feat(eft): localStorage 저장소(createStore) + 테스트"
```

---

### Task 5: HTML 셸 · 스타일시트

**Files:**
- Create: `<APP>/index.html`
- Create: `<APP>/css/styles.css`

**Interfaces:**
- Produces: `#app`(뷰 렌더 대상), `#topnav`(네비 버튼 대상) DOM 컨테이너. `js/app.js`를 `type="module"`로 로드.

- [ ] **Step 1: index.html 작성**

`<APP>/index.html`:
```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EFT 정서케어 트래커</title>
  <link rel="stylesheet" href="css/styles.css">
</head>
<body>
  <header class="topbar">
    <span class="brand">EFT 정서케어 트래커</span>
    <nav class="topnav" id="topnav"></nav>
  </header>
  <main id="app"></main>
  <footer class="footer">
    <small>화병 증상 문항 출처: 권정혜·김종우, 화병척도 · 본 도구는 의료 진단·치료를 대체하지 않습니다.</small>
  </footer>
  <script type="module" src="js/app.js"></script>
</body>
</html>
```

- [ ] **Step 2: styles.css 작성**

`<APP>/css/styles.css`:
```css
:root {
  --bg: #f5f7f6;
  --card: #ffffff;
  --ink: #23302c;
  --muted: #6b7a74;
  --brand: #3f7d6e;
  --brand-ink: #2c5a4f;
  --accent: #c98a5e;
  --good: #2e8b57;
  --bad: #c0554a;
  --line: #e3e9e6;
}
* { box-sizing: border-box; }
body {
  margin: 0;
  font-family: system-ui, "Segoe UI", "Malgun Gothic", sans-serif;
  color: var(--ink);
  background: var(--bg);
  line-height: 1.5;
}
.topbar {
  display: flex; align-items: center; justify-content: space-between;
  gap: 12px; padding: 12px 16px; background: var(--brand); color: #fff;
  position: sticky; top: 0; z-index: 10; flex-wrap: wrap;
}
.brand { font-weight: 700; }
.topnav { display: flex; gap: 6px; }
.navbtn {
  border: 0; background: rgba(255,255,255,.15); color: #fff;
  padding: 6px 12px; border-radius: 999px; cursor: pointer; font-size: 14px;
}
.navbtn.active { background: #fff; color: var(--brand-ink); font-weight: 700; }
#app { max-width: 560px; margin: 0 auto; padding: 16px; }
.view h1 { font-size: 22px; margin: 8px 0 4px; }
.view h2 { font-size: 17px; margin: 0 0 8px; }
.lead { color: var(--muted); margin: 0 0 16px; }
.card {
  background: var(--card); border: 1px solid var(--line);
  border-radius: 14px; padding: 16px; margin-bottom: 14px;
}
.card.banner { display: flex; align-items: center; justify-content: space-between; gap: 10px; }
button.primary {
  background: var(--brand); color: #fff; border: 0; border-radius: 10px;
  padding: 10px 16px; cursor: pointer; font-size: 15px;
}
button.primary[disabled] { opacity: .45; cursor: not-allowed; }
button.primary.big { width: 100%; padding: 14px; font-size: 16px; margin-top: 8px; }
button.ghost {
  background: transparent; color: var(--brand-ink); border: 1px solid var(--brand);
  border-radius: 10px; padding: 10px 16px; cursor: pointer; font-size: 15px;
}
.row { display: flex; gap: 10px; }
.row.cta { margin: 12px 0; }
.row.cta button { flex: 1; }
.howto, .guide-steps, .tap-list { margin: 0; padding-left: 18px; }
.howto li, .guide-steps li, .tap-list li { margin: 4px 0; }
.hint { color: var(--muted); font-size: 13px; }
.source { color: var(--muted); font-size: 12px; margin-top: 12px; }
.field { display: block; margin: 10px 0; }
.field > span { display: block; font-size: 13px; color: var(--muted); margin-bottom: 4px; }
.field input, .field textarea {
  width: 100%; padding: 9px 11px; border: 1px solid var(--line);
  border-radius: 9px; font: inherit; background: #fff;
}
.q-list { margin-bottom: 14px; }
.q-item { background: var(--card); border: 1px solid var(--line); border-radius: 12px; padding: 12px; margin-bottom: 10px; }
.q-text { font-size: 15px; margin-bottom: 8px; }
.q-options { display: flex; gap: 6px; }
.opt {
  flex: 1; display: flex; flex-direction: column; align-items: center; gap: 2px;
  padding: 8px 0; border: 1px solid var(--line); border-radius: 9px; cursor: pointer;
}
.opt input { margin: 0; }
.status { text-align: center; color: var(--muted); font-size: 14px; }
.stepbar { color: var(--brand-ink); font-weight: 700; margin-bottom: 10px; }
.nrs { display: flex; align-items: center; gap: 10px; }
.nrs input[type=range] { flex: 1; }
.nrs output { min-width: 28px; text-align: center; font-weight: 700; color: var(--brand-ink); }
.result .delta { font-size: 20px; font-weight: 700; color: var(--brand-ink); }
.stats { display: flex; gap: 10px; margin-bottom: 8px; }
.stat { flex: 1; background: var(--card); border: 1px solid var(--line); border-radius: 12px; padding: 12px; text-align: center; }
.stat-value { font-size: 20px; font-weight: 700; color: var(--brand-ink); }
.stat-label { font-size: 12px; color: var(--muted); }
.empty { color: var(--muted); }
.chart { width: 100%; height: auto; background: var(--card); border: 1px solid var(--line); border-radius: 12px; }
.chart .bar { fill: var(--brand); }
.chart .bar-label { fill: var(--muted); font-size: 8px; }
.session-list { display: flex; flex-direction: column; gap: 8px; }
.session-item {
  display: flex; align-items: center; gap: 10px; background: var(--card);
  border: 1px solid var(--line); border-radius: 10px; padding: 10px 12px;
}
.session-item .emotion { flex: 1; font-weight: 600; }
.session-item .ba { color: var(--muted); }
.session-item .delta { font-weight: 700; min-width: 34px; text-align: right; }
.session-item .delta.good { color: var(--good); }
.session-item .delta.bad { color: var(--bad); }
.footer { text-align: center; color: var(--muted); padding: 20px 16px 40px; }
```

- [ ] **Step 3: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/index.html" "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/css/styles.css"
git commit -m "feat(eft): HTML 셸 + 스타일시트"
```

---

### Task 6: 라우터 (app.js)

**Files:**
- Create: `<APP>/js/app.js`
- Create: `<APP>/js/views/onboarding.js` (임시 스텁 — Task 7에서 완성)
- Create: `<APP>/js/views/guide.js` (임시 스텁 — Task 8에서 완성)
- Create: `<APP>/js/views/dailyCheck.js` (임시 스텁 — Task 9에서 완성)
- Create: `<APP>/js/views/session.js` (임시 스텁 — Task 10에서 완성)
- Create: `<APP>/js/views/dashboard.js` (임시 스텁 — Task 11에서 완성)

**Interfaces:**
- Consumes: `createStore` (storage.js), `el` (util.js), 각 뷰의 `renderX(container, ctx)`
- Produces: `navigate(name)` 라우팅. `ctx = { store, navigate }`를 각 뷰에 전달. 뷰 이름: `onboarding | guide | daily | session | dashboard`

- [ ] **Step 1: 뷰 스텁 5개 작성**

각 파일은 아래 형태의 스텁으로 만든다(다음 태스크에서 내용 채움). 파일명·export명은 반드시 아래대로.

`<APP>/js/views/onboarding.js`:
```js
import { el } from '../util.js';
export function renderOnboarding(root, ctx) {
  root.appendChild(el('section', { class: 'view' }, el('h1', { text: 'onboarding (stub)' })));
}
```

`<APP>/js/views/guide.js`:
```js
import { el } from '../util.js';
export function renderGuide(root, ctx) {
  root.appendChild(el('section', { class: 'view' }, el('h1', { text: 'guide (stub)' })));
}
```

`<APP>/js/views/dailyCheck.js`:
```js
import { el } from '../util.js';
export function renderDailyCheck(root, ctx) {
  root.appendChild(el('section', { class: 'view' }, el('h1', { text: 'daily (stub)' })));
}
```

`<APP>/js/views/session.js`:
```js
import { el } from '../util.js';
export function renderSession(root, ctx) {
  root.appendChild(el('section', { class: 'view' }, el('h1', { text: 'session (stub)' })));
}
```

`<APP>/js/views/dashboard.js`:
```js
import { el } from '../util.js';
export function renderDashboard(root, ctx) {
  root.appendChild(el('section', { class: 'view' }, el('h1', { text: 'dashboard (stub)' })));
}
```

- [ ] **Step 2: app.js 작성**

`<APP>/js/app.js`:
```js
import { createStore } from './storage.js';
import { el } from './util.js';
import { renderOnboarding } from './views/onboarding.js';
import { renderGuide } from './views/guide.js';
import { renderDailyCheck } from './views/dailyCheck.js';
import { renderSession } from './views/session.js';
import { renderDashboard } from './views/dashboard.js';

const store = createStore(window.localStorage);
const root = document.getElementById('app');
const topnav = document.getElementById('topnav');

const views = {
  onboarding: renderOnboarding,
  guide: renderGuide,
  daily: renderDailyCheck,
  session: renderSession,
  dashboard: renderDashboard,
};

function renderNav(current) {
  topnav.innerHTML = '';
  const items = [['dashboard', '홈'], ['daily', '자가진단'], ['session', 'EFT 실천']];
  for (const [name, label] of items) {
    topnav.appendChild(el('button', {
      class: 'navbtn' + (name === current ? ' active' : ''),
      onClick: () => navigate(name),
      text: label,
    }));
  }
}

function navigate(name) {
  root.innerHTML = '';
  const showNav = name !== 'onboarding';
  topnav.style.display = showNav ? '' : 'none';
  if (showNav) renderNav(name);
  const ctx = { store, navigate };
  (views[name] || views.dashboard)(root, ctx);
}

const state = store.load();
navigate(state.onboardingDone ? 'dashboard' : 'onboarding');
```

- [ ] **Step 3: 브라우저에서 라우팅 수동 검증**

로컬 서버 실행: `cd "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker" && python -m http.server 8000`
(또는 mcp Browser의 preview_start로 정적 서버를 띄운다.)
브라우저에서 `http://localhost:8000` 접속.
- Expected: localStorage가 비어 있으면 상단 네비 없이 "onboarding (stub)" 표시.
- 개발자도구 콘솔에서 `localStorage.setItem('eft_tracker_v1', JSON.stringify({onboardingDone:true}))` 실행 후 새로고침 → 상단 네비(홈/자가진단/EFT 실천) 보이고 "dashboard (stub)" 표시.
- 네비 버튼 클릭 시 각 스텁 화면으로 전환되는지 확인.
- 콘솔 에러 없어야 함(모듈 import 실패 등).

- [ ] **Step 4: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/app.js" "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/views"
git commit -m "feat(eft): 뷰 라우터(app.js) + 뷰 스텁 5개"
```

---

### Task 7: 온보딩 화면 (onboarding.js)

**Files:**
- Modify: `<APP>/js/views/onboarding.js` (스텁 → 완성)

**Interfaces:**
- Consumes: `el` (util.js), `ctx.store.setOnboardingDone()`, `ctx.navigate('dashboard')`

- [ ] **Step 1: onboarding.js 완성**

`<APP>/js/views/onboarding.js` 전체를 아래로 교체:
```js
import { el } from '../util.js';

export function renderOnboarding(root, ctx) {
  root.appendChild(el('section', { class: 'view onboarding' },
    el('h1', { text: 'EFT 정서케어 트래커' }),
    el('p', { class: 'lead', text: '침 없이, 손끝으로 타점을 두드려 감정을 다스리는 오픈소스 자가치료법(EFT)입니다.' }),
    el('div', { class: 'card' },
      el('h2', { text: '누가 만들었나요?' }),
      el('p', { text: '17년차 한의사 · 암 한방병원장이 임상 경험을 바탕으로 설계했습니다. 매일의 감정을 스스로 체크하고, 그 자리에서 다스린 뒤 변화를 눈으로 확인하는 습관을 돕습니다.' }),
    ),
    el('div', { class: 'card' },
      el('h2', { text: '어떻게 쓰나요?' }),
      el('ol', { class: 'howto' },
        el('li', { text: '오늘의 감정 상태를 자가진단(15문항)합니다.' }),
        el('li', { text: 'EFT 8단계를 따라 감정을 다스립니다.' }),
        el('li', { text: '전후 변화가 숫자로 기록되고, 추이가 그래프로 쌓입니다.' }),
      ),
    ),
    el('button', {
      class: 'primary big',
      onClick: () => { ctx.store.setOnboardingDone(); ctx.navigate('dashboard'); },
      text: '시작하기',
    }),
  ));
}
```

- [ ] **Step 2: 브라우저 수동 검증**

먼저 콘솔에서 `localStorage.clear()` 후 새로고침.
서버: `cd "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker" && python -m http.server 8000` → `http://localhost:8000`
- Expected: "마음침" 단어가 어디에도 없음. 프로필/사용법 카드 표시.
- "시작하기" 클릭 → 대시보드(stub 또는 완성본)로 이동하고, 새로고침해도 온보딩으로 돌아가지 않음(localStorage에 onboardingDone 저장됨).

- [ ] **Step 3: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/views/onboarding.js"
git commit -m "feat(eft): 온보딩 화면(전문가 신뢰 소구)"
```

---

### Task 8: EFT 안내 화면 (guide.js)

**Files:**
- Modify: `<APP>/js/views/guide.js` (스텁 → 완성)

**Interfaces:**
- Consumes: `el` (util.js), `EFT_TAP_POINTS`·`EFT_GUIDE_STEPS` (data.js), `ctx.store.setEftGuideSeen()`, `ctx.navigate('session'|'dashboard')`

- [ ] **Step 1: guide.js 완성**

`<APP>/js/views/guide.js` 전체를 아래로 교체:
```js
import { el } from '../util.js';
import { EFT_TAP_POINTS, EFT_GUIDE_STEPS } from '../data.js';

export function renderGuide(root, ctx) {
  root.appendChild(el('section', { class: 'view guide' },
    el('h1', { text: 'EFT 처음이신가요?' }),
    el('div', { class: 'card' },
      el('ul', { class: 'guide-steps' },
        ...EFT_GUIDE_STEPS.map((s) => el('li', { text: s })),
      ),
    ),
    el('div', { class: 'card' },
      el('h2', { text: '타점 순서' }),
      el('ol', { class: 'tap-list' },
        ...EFT_TAP_POINTS.map((p) => el('li', {},
          el('strong', { text: p.name }),
          el('span', { class: 'hint', text: ' — ' + p.hint }),
        )),
      ),
    ),
    el('div', { class: 'row' },
      el('button', { class: 'ghost', onClick: () => ctx.navigate('dashboard'), text: '나중에' }),
      el('button', {
        class: 'primary',
        onClick: () => { ctx.store.setEftGuideSeen(); ctx.navigate('session'); },
        text: '이해했어요, EFT 시작',
      }),
    ),
  ));
}
```

- [ ] **Step 2: 브라우저 수동 검증**

서버 실행 후 대시보드에서 "방법 보기"(Task 11에서 생김) 또는 콘솔에서 직접 확인이 어려우면, 임시로 `js/app.js`의 마지막 줄을 `navigate('guide')`로 바꿔 확인 후 되돌린다.
- Expected: EFT 안내 5줄 + 타점 9개(손날점 준비 포함) 순서 표시. "이해했어요" 클릭 시 EFT 실천 화면으로 이동.

- [ ] **Step 3: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/views/guide.js"
git commit -m "feat(eft): EFT 안내 화면(타점 순서)"
```

---

### Task 9: 자가진단 화면 (dailyCheck.js)

**Files:**
- Modify: `<APP>/js/views/dailyCheck.js` (스텁 → 완성)

**Interfaces:**
- Consumes: `el`·`todayStr` (util.js), `SYMPTOM_QUESTIONS`·`SYMPTOM_SCALE_LABELS` (data.js), `computeDailyTotal` (scoring.js), `ctx.store.addDailyCheck(...)`, `ctx.navigate('dashboard')`

- [ ] **Step 1: dailyCheck.js 완성**

`<APP>/js/views/dailyCheck.js` 전체를 아래로 교체:
```js
import { el, todayStr } from '../util.js';
import { SYMPTOM_QUESTIONS, SYMPTOM_SCALE_LABELS } from '../data.js';
import { computeDailyTotal } from '../scoring.js';

export function renderDailyCheck(root, ctx) {
  const scores = new Array(SYMPTOM_QUESTIONS.length).fill(null);

  const section = el('section', { class: 'view daily' },
    el('h1', { text: '오늘의 자가진단' }),
    el('p', { class: 'lead', text: '지금 내 상태에 얼마나 해당하나요? (0 전혀 그렇지 않다 ~ 4 완전히 그렇다)' }),
  );

  const status = el('p', { class: 'status', text: '' });
  const saveBtn = el('button', { class: 'primary big', disabled: 'true', text: '저장하기' });

  function updateStatus() {
    const done = scores.filter((s) => s !== null).length;
    status.textContent = `${done} / ${scores.length} 문항 응답`;
    if (done === scores.length) saveBtn.removeAttribute('disabled');
    else saveBtn.setAttribute('disabled', 'true');
  }

  const list = el('div', { class: 'q-list' });
  SYMPTOM_QUESTIONS.forEach((q, qi) => {
    const options = el('div', { class: 'q-options' });
    SYMPTOM_SCALE_LABELS.forEach((label, val) => {
      const id = `q${qi}_v${val}`;
      const input = el('input', { type: 'radio', name: `q${qi}`, id, value: String(val) });
      input.addEventListener('change', () => { scores[qi] = val; updateStatus(); });
      options.appendChild(el('label', { class: 'opt', for: id, title: label }, input, el('span', { text: String(val) })));
    });
    list.appendChild(el('div', { class: 'q-item' },
      el('div', { class: 'q-text', text: `${qi + 1}. ${q}` }),
      options,
    ));
  });
  section.appendChild(list);

  saveBtn.addEventListener('click', () => {
    const total = computeDailyTotal(scores);
    ctx.store.addDailyCheck({ date: todayStr(), symptomScores: scores.slice(), total });
    ctx.navigate('dashboard');
  });

  section.appendChild(status);
  section.appendChild(saveBtn);
  section.appendChild(el('p', { class: 'source', text: '문항 출처: 권정혜·김종우, 화병척도' }));
  updateStatus();
  root.appendChild(section);
}
```

- [ ] **Step 2: 브라우저 수동 검증**

서버 실행 후 대시보드 → "오늘 자가진단" 또는 네비 "자가진단" 클릭.
- Expected: 15문항 각각 0~4 라디오. 응답 수 카운터가 "n / 15"로 갱신. 15개 모두 채우기 전엔 저장 버튼 비활성.
- 15개 채우고 저장 → 대시보드로 이동, 총점 추이에 오늘 막대가 생김. 하단에 출처 문구 표시.

- [ ] **Step 3: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/views/dailyCheck.js"
git commit -m "feat(eft): 자가진단 화면(화병 증상 15문항, 0~4)"
```

---

### Task 10: EFT 실천 화면 (session.js)

**Files:**
- Modify: `<APP>/js/views/session.js` (스텁 → 완성)

**Interfaces:**
- Consumes: `el` (util.js), `SHAPE_FIELDS`·`EFT_TAP_POINTS` (data.js), `nrsDelta` (scoring.js), `ctx.store.addSession(...)`, `ctx.navigate('dashboard')`

- [ ] **Step 1: session.js 완성**

`<APP>/js/views/session.js` 전체를 아래로 교체:
```js
import { el } from '../util.js';
import { SHAPE_FIELDS, EFT_TAP_POINTS } from '../data.js';
import { nrsDelta } from '../scoring.js';

export function renderSession(root, ctx) {
  const draft = {
    situation: '', emotions: '', chosenEmotion: '',
    before: 5, after: 5,
    shape: { temperature: '', texture: '', color: '', form: '', size: '', weight: '' },
    bodyLocation: '',
  };
  let step = 1;
  const TOTAL = 8;

  const container = el('section', { class: 'view session' });
  root.appendChild(container);
  render();

  function render() {
    container.innerHTML = '';
    container.appendChild(el('div', { class: 'stepbar', text: `EFT 실천 · ${step} / ${TOTAL} 단계` }));
    container.appendChild(stepBody());
    container.appendChild(controls());
  }

  function controls() {
    const row = el('div', { class: 'row' });
    if (step > 1 && step <= TOTAL) {
      row.appendChild(el('button', { class: 'ghost', onClick: () => { step -= 1; render(); }, text: '이전' }));
    }
    if (step < TOTAL) {
      row.appendChild(el('button', { class: 'primary', onClick: () => { if (validate()) { step += 1; render(); } }, text: '다음' }));
    }
    return row;
  }

  function validate() {
    if (step === 2 && !draft.chosenEmotion.trim()) {
      alert('대표 감정을 하나 적어주세요.');
      return false;
    }
    return true;
  }

  function slider(key) {
    const out = el('output', { text: String(draft[key]) });
    const input = el('input', { type: 'range', min: '0', max: '10', step: '1', value: String(draft[key]) });
    input.addEventListener('input', () => { draft[key] = Number(input.value); out.textContent = input.value; });
    return el('div', { class: 'nrs' }, el('span', { text: '0' }), input, el('span', { text: '10' }), out);
  }

  function textField(label, get, set, placeholder = '') {
    const input = el('input', { type: 'text', value: get(), placeholder });
    input.addEventListener('input', () => set(input.value));
    return el('label', { class: 'field' }, el('span', { text: label }), input);
  }

  function textArea(label, get, set, placeholder = '') {
    const ta = el('textarea', { rows: '3', placeholder });
    ta.value = get();
    ta.addEventListener('input', () => set(ta.value));
    return el('label', { class: 'field' }, el('span', { text: label }), ta);
  }

  function stepBody() {
    switch (step) {
      case 1:
        return el('div', { class: 'card' },
          el('h2', { text: '1. 어떤 상황인가요?' }),
          textArea('불쾌한 감정이 떠오르는 상황', () => draft.situation, (v) => { draft.situation = v; }, '예: 오늘 회의에서 지적받았을 때'),
          textArea('그때 어떤 감정들이 드나요?', () => draft.emotions, (v) => { draft.emotions = v; }, '예: 억울함, 답답함, 화'),
        );
      case 2:
        return el('div', { class: 'card' },
          el('h2', { text: '2. 가장 큰 감정 하나를 고르세요' }),
          textField('대표 감정', () => draft.chosenEmotion, (v) => { draft.chosenEmotion = v; }, '예: 억울함'),
        );
      case 3:
        return el('div', { class: 'card' },
          el('h2', { text: '3. 지금 그 감정은 얼마나 강한가요?' }),
          el('p', { class: 'hint', text: '0(전혀) ~ 10(매우 강함)' }),
          slider('before'),
        );
      case 4:
        return el('div', { class: 'card' },
          el('h2', { text: '4. 그 감정을 몸의 느낌으로 그려보세요' }),
          el('p', { class: 'hint', text: '구체적으로 상상할수록 효과가 커집니다. 떠오르는 대로 적어보세요.' }),
          ...SHAPE_FIELDS.map((f) => textField(f.label, () => draft.shape[f.key], (v) => { draft.shape[f.key] = v; }, f.placeholder)),
        );
      case 5:
        return el('div', { class: 'card' },
          el('h2', { text: '5. 그 느낌은 몸의 어디에 있나요?' }),
          textField('몸의 위치', () => draft.bodyLocation, (v) => { draft.bodyLocation = v; }, '예: 가슴 한복판, 명치, 목'),
        );
      case 6:
        return el('div', { class: 'card' },
          el('h2', { text: '6. 호흡하기' }),
          el('p', { text: '그 감정이 있는 곳을 마음속으로 바라보며, 깊은 호흡을 천천히 3회 반복하세요.' }),
        );
      case 7:
        return el('div', { class: 'card' },
          el('h2', { text: '7. EFT 타점 두드리기' }),
          el('p', { class: 'hint', text: '각 타점을 검지·중지로 5~7회 두드리며, 그 감정에 집중하세요.' }),
          el('ol', { class: 'tap-list' }, ...EFT_TAP_POINTS.map((p) => el('li', {},
            el('strong', { text: p.name }), el('span', { class: 'hint', text: ' — ' + p.hint }),
          ))),
        );
      case 8:
        return el('div', { class: 'card' },
          el('h2', { text: '8. 다시 느껴보고 강도를 확인하세요' }),
          el('p', { class: 'hint', text: `처음엔 ${draft.before}점이었어요. 지금은?` }),
          slider('after'),
          el('button', { class: 'primary big', onClick: saveSession, text: '기록 저장' }),
        );
      default:
        return el('div', {});
    }
  }

  function saveSession() {
    const session = {
      datetime: new Date().toISOString(),
      situation: draft.situation,
      emotions: draft.emotions,
      chosenEmotion: draft.chosenEmotion,
      before: draft.before,
      shape: { ...draft.shape },
      bodyLocation: draft.bodyLocation,
      after: draft.after,
    };
    ctx.store.addSession(session);
    const delta = nrsDelta(draft.before, draft.after);
    const deltaText = delta > 0 ? `-${delta}` : delta < 0 ? `+${-delta}` : '0';
    container.innerHTML = '';
    container.appendChild(el('div', { class: 'card result' },
      el('h2', { text: '기록됐어요' }),
      el('p', { class: 'delta', text: `${draft.chosenEmotion}: ${draft.before} → ${draft.after} (${deltaText})` }),
      el('button', { class: 'primary', onClick: () => ctx.navigate('dashboard'), text: '홈으로' }),
    ));
  }
}
```

- [ ] **Step 2: 브라우저 수동 검증**

서버 실행 후 네비 "EFT 실천" 클릭. 8단계를 순서대로 진행.
- Expected: 1(상황/감정) → 2(대표 감정, 미입력 시 다음 막힘) → 3(before 슬라이더 0~10) → 4(형상화 6칸) → 5(몸 위치) → 6(호흡 안내) → 7(타점 9개) → 8(after 슬라이더 + 기록 저장).
- "기록 저장" 시 "감정: before → after (−n)" 결과 카드 표시. "홈으로" → 대시보드 "EFT 전후 변화"에 방금 세션이 추가됨.

- [ ] **Step 3: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/views/session.js"
git commit -m "feat(eft): EFT 8단계 실천 화면(Before/After NRS + 감정 형상화)"
```

---

### Task 11: 대시보드 화면 (dashboard.js)

**Files:**
- Modify: `<APP>/js/views/dashboard.js` (스텁 → 완성)

**Interfaces:**
- Consumes: `el`·`todayStr` (util.js), `computeStreak` (streak.js), `ctx.store.load()`, `ctx.navigate('daily'|'session'|'guide')`

- [ ] **Step 1: dashboard.js 완성**

`<APP>/js/views/dashboard.js` 전체를 아래로 교체:
```js
import { el, todayStr } from '../util.js';
import { computeStreak } from '../streak.js';

function barChart(data, maxValue) {
  const W = 320, H = 120, pad = 20;
  const n = data.length;
  const bw = n > 0 ? (W - pad * 2) / n : 0;
  const NS = 'http://www.w3.org/2000/svg';
  const svg = document.createElementNS(NS, 'svg');
  svg.setAttribute('viewBox', `0 0 ${W} ${H}`);
  svg.setAttribute('class', 'chart');
  data.forEach((d, i) => {
    const h = maxValue > 0 ? (d.value / maxValue) * (H - pad * 2) : 0;
    const x = pad + i * bw + bw * 0.15;
    const y = H - pad - h;
    const rect = document.createElementNS(NS, 'rect');
    rect.setAttribute('x', String(x));
    rect.setAttribute('y', String(y));
    rect.setAttribute('width', String(bw * 0.7));
    rect.setAttribute('height', String(h));
    rect.setAttribute('rx', '2');
    rect.setAttribute('class', 'bar');
    svg.appendChild(rect);
    const label = document.createElementNS(NS, 'text');
    label.setAttribute('x', String(x + bw * 0.35));
    label.setAttribute('y', String(H - pad + 12));
    label.setAttribute('text-anchor', 'middle');
    label.setAttribute('class', 'bar-label');
    label.textContent = d.label;
    svg.appendChild(label);
  });
  return svg;
}

function stat(label, value) {
  return el('div', { class: 'stat' },
    el('div', { class: 'stat-value', text: value }),
    el('div', { class: 'stat-label', text: label }),
  );
}

export function renderDashboard(root, ctx) {
  const state = ctx.store.load();
  const section = el('section', { class: 'view dashboard' });

  if (!state.eftGuideSeen) {
    section.appendChild(el('div', { class: 'card banner' },
      el('span', { text: 'EFT가 처음이신가요? 먼저 방법을 볼 수 있어요.' }),
      el('button', { class: 'ghost', onClick: () => ctx.navigate('guide'), text: '방법 보기' }),
    ));
  }

  const streak = computeStreak(state.dailyChecks, todayStr());
  section.appendChild(el('div', { class: 'stats' },
    stat('연속 실천', `${streak}일`),
    stat('자가진단', `${state.dailyChecks.length}회`),
    stat('EFT 세션', `${state.eftSessions.length}회`),
  ));

  section.appendChild(el('div', { class: 'row cta' },
    el('button', { class: 'primary', onClick: () => ctx.navigate('daily'), text: '오늘 자가진단' }),
    el('button', { class: 'primary', onClick: () => ctx.navigate('session'), text: 'EFT 실천하기' }),
  ));

  section.appendChild(el('h2', { text: '화병 증상 총점 추이' }));
  if (state.dailyChecks.length === 0) {
    section.appendChild(el('p', { class: 'empty', text: '아직 자가진단 기록이 없어요.' }));
  } else {
    const data = state.dailyChecks.map((c) => ({ label: c.date.slice(5), value: c.total }));
    section.appendChild(barChart(data, 60));
  }

  section.appendChild(el('h2', { text: 'EFT 전후 변화' }));
  if (state.eftSessions.length === 0) {
    section.appendChild(el('p', { class: 'empty', text: '아직 EFT 기록이 없어요.' }));
  } else {
    const list = el('div', { class: 'session-list' });
    state.eftSessions.slice().reverse().forEach((s) => {
      const delta = s.before - s.after;
      const cls = delta > 0 ? 'good' : delta < 0 ? 'bad' : '';
      const deltaText = delta > 0 ? `-${delta}` : delta < 0 ? `+${-delta}` : '0';
      list.appendChild(el('div', { class: 'session-item' },
        el('span', { class: 'emotion', text: s.chosenEmotion || '(감정)' }),
        el('span', { class: 'ba', text: `${s.before} → ${s.after}` }),
        el('span', { class: 'delta ' + cls, text: deltaText }),
      ));
    });
    section.appendChild(list);
  }

  root.appendChild(section);
}
```

- [ ] **Step 2: 브라우저 수동 검증(전체 흐름)**

콘솔에서 `localStorage.clear()` 후 새로고침하여 처음부터 진행:
- Expected: 온보딩 → 시작하기 → 대시보드. 대시보드에 "방법 보기" 배너(아직 guide 안 봄), 통계 3개(연속 0일/진단 0회/세션 0회), 빈 상태 문구 2개.
- "오늘 자가진단" 15문항 저장 → 대시보드 총점 막대 1개, 연속 1일, 진단 1회.
- "EFT 실천하기" 8단계 완료 → 대시보드 "EFT 전후 변화"에 항목 1개, delta 색상(호전=초록 −n, 악화=빨강 +n).
- "방법 보기" → guide → "이해했어요" 후 대시보드 재방문 시 배너 사라짐(eftGuideSeen).
- 콘솔 에러 없음.

- [ ] **Step 3: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/js/views/dashboard.js"
git commit -m "feat(eft): 대시보드(총점 추이 SVG + EFT 전후 변화 + 스트릭)"
```

---

### Task 12: README · 전체 검증 · 마무리

**Files:**
- Create: `<APP>/README.md`
- (참고) 스크린샷 저장: `1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/이미지첨부/`

**Interfaces:** 없음(문서·검증·배포 안내)

- [ ] **Step 1: 전체 테스트 재실행**

Run: `cd "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker" && node --test`
Expected: PASS (20 tests: util 2 + scoring 6 + streak 7 + storage 5)

- [ ] **Step 2: README 작성**

`<APP>/README.md`:
```markdown
# EFT 정서케어 트래커

화병 증상 자가진단(0~4)과 EFT 8단계 자가치료(Before/After NRS 0~10)를 기록하고
변화 추이를 보여주는 정적 웹앱 프로토타입. 스폰지클럽 2기 4주차 과제.

- 만든 사람: 자인(허정우) · 3조
- 데이터는 브라우저 localStorage에만 저장(서버·계정 없음)
- 외부 라이브러리 없음(순수 HTML/CSS/JS, 그래프는 인라인 SVG)

## 로컬 실행

ES 모듈을 쓰므로 `file://`이 아니라 로컬 서버로 열어야 합니다.

```
cd eft-tracker
python -m http.server 8000
# 브라우저에서 http://localhost:8000
```

## 테스트

```
cd eft-tracker
node --test
```

## 화면

1. 온보딩 — EFT 소개 + 만든 사람(전문가) 소개
2. 자가진단 — 화병 증상 15문항(0~4)
3. EFT 안내 — 처음 사용자용 타점 안내
4. EFT 실천 — 8단계(상황→대표감정→강도 before→형상화→몸 위치→호흡→타점→강도 after)
5. 대시보드 — 총점 추이, EFT 전후 변화, 연속 실천일수

## 참고·저작권

- 화병 증상 문항 출처: 권정혜·김종우, 화병척도
- "마음침" 표현은 원 개발자 저작권 이슈로 사용하지 않고, 오픈소스인 EFT로만 구성
- Beck 척도(BAI/BDI)는 상업 라이선스 문제로 미사용
- 본 도구는 의료 진단·치료를 대체하지 않습니다.
```

- [ ] **Step 3: 배포(피드백용 링크) — 선택**

정적 파일이므로 아래 중 하나로 배포해 실제 유저 2명에게 링크 공유:
- GitHub Pages(리포지토리 설정 필요), Netlify Drop, Vercel 등 정적 호스팅.
- 주의: ES 모듈은 http(s)에서만 로드됨(`file://` 직접 열기 불가). 배포 URL이나 로컬 서버로 접속해야 함.
배포가 여의치 않으면 로컬 서버(`python -m http.server`) 화면을 공유하거나, 화면 녹화/스크린샷으로 피드백을 받는다.

- [ ] **Step 4: 스크린샷 저장(과제 제출용)**

브라우저에서 각 화면 스크린샷을 찍어 `1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/이미지첨부/`에 저장한다(온보딩/자가진단/EFT 실천/대시보드).

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/jayhu/spongeclub-2"
git add "1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/eft-tracker/README.md"
git commit -m "docs(eft): README + 로컬 실행/테스트/배포 안내"
```

---

## 참고: submission.md 채우기 (구현 완료 후 별도 작업)

구현이 끝나면 `1_mission/3조/자인(허정우)/4주차 과제 제출 폴더/submission.md`의 과제 1(PRD)·과제 2(유저 피드백)·과제 3(SNS)을 채운다. 이는 구현 태스크가 아니라 실제 유저 피드백을 받은 뒤 진행하는 별도 단계다.
