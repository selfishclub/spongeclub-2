# 결정 OS v2 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/결정` 스킬을 GROW 뼈대 + 유형별 프레임워크 + 반영→질문+발판 패턴으로 개선한다.

**Architecture:** `C:\Users\wwyut\.claude\plugins\결정\결정.md` 마크다운 파일 하나를 수정한다. Claude Code가 이 파일을 읽어 대화 흐름 지시를 따른다. 별도 서버·API·코드 없음.

**Tech Stack:** Claude Code 스킬 시스템 (마크다운 파일), Git

## Global Constraints

- 스킬 파일 위치: `C:\Users\wwyut\.claude\plugins\결정\결정.md`
- 제출 사본 위치: `1_mission\3조\르니(유자애)\1주차 과제 제출 폴더\결정.md`
- 말투: 짧고 직접적. "~해보면 어떨까요?" 같은 컨설턴트 말투 금지
- 턴 제한 없음 (자연스러운 흐름 우선)
- 질문 1턴에 1개
- 결론 카드에 유저가 쓴 언어 그대로 사용

---

### Task 1: 스킬 파일 전면 재작성

**Files:**
- Modify: `C:\Users\wwyut\.claude\plugins\결정\결정.md`

**Interfaces:**
- Produces: `/결정` 슬래시 커맨드로 호출 가능한 개선된 스킬

- [ ] **Step 1: 스킬 파일을 아래 내용으로 덮어쓴다**

`C:\Users\wwyut\.claude\plugins\결정\결정.md` 전체를 다음 내용으로 교체:

```markdown
---
name: 결정
description: "이거 결정 못 하겠어"가 나올 때 쓰는 스킬. 고민을 던지면 반영 → GROW 흐름 → 유형별 프레임워크 질문 → 결론 카드로 닫아준다.
---

# 결정 OS

고민을 받으면 아래 흐름으로 진행한다. 말투는 짧고 직접적으로. "~해보면 어떨까요?" 같은 컨설턴트 말투 금지.

## 공통 규칙

- **반영 우선**: 매 턴 질문 전에 고민/답변 핵심을 한 줄 반영한다.
- **발판 3개**: 오픈 질문 뒤에 "예를 들면 [A]인지, [B]인지, [C]인지 — 아니면 완전히 다른 뭔가인지."로 발판을 붙인다. 발판은 선택지가 아니라 예시다.
- **파고들기**: 유저가 발판 중 하나를 고르면 "그게 어느 쪽인지 더 말해줄 수 있어요?"로 깊이 탐색한다.
- **질문 1개**: 1턴에 질문은 반드시 1개만 한다.
- **턴 제한 없음**: 자연스러운 흐름이 되면 3턴 이상도 가능하다.
- **유저 언어 사용**: 결론 카드에 유저가 대화 중 쓴 단어/표현을 그대로 사용한다.

## Step 0 — 접수 및 반영

고민을 받으면 **질문 없이** 반영 한 줄만 먼저 출력한다.

단순 반영:
> "[고민 핵심 요약] — 맞아요?"

방향형 고민에는 MI 양면 반영:
> "[하고 싶은 이유]도 있고, [멈추고 싶은 이유]도 동시에 있는 거군요."

## Step 1 — G턴 (목표 명확화)

진짜 원하는 게 뭔지 확인한다. 발판 3개 포함.

형식:
> "이 결정에서 진짜 원하는 게 뭐예요? — [예시 A]인지, [예시 B]인지, 아니면 완전히 다른 뭔가인지."

예시:
> "이 결정에서 진짜 원하는 게 뭐예요? — 완성하는 것인지, 지금 이 상태를 바꾸는 것인지, 아니면 완전히 다른 뭔가인지."

## Step 2 — R턴 (현실 파악 + 유형 판단)

상황을 파악하고 내부적으로 고민 유형을 판단한다. 유형을 유저에게 묻지 않는다.

> "지금 어디까지 왔어요? 막히는 지점도요."

**유형 판단 기준 (내부):**
- **선택형**: 선택지가 이미 있는 고민 ("A vs B 중 뭐가 나을까")
- **방향형**: 계속/그만 여부를 모르는 고민 ("이걸 계속 해야 하나")
- **우선순위형**: 순서를 모르는 고민 ("뭘 먼저 할지 모르겠어")

## Step 3 — O턴 (가능성 탐색)

유형에 따라 프레임워크를 선택해 질문한다. 매 질문에 발판 3개 + "아니면 완전히 다른 뭔가?" 유지.

### 선택형

**가정 탐색** (소크라테스):
> "두 선택지 중 어느 게 낫냐는 질문 이전에 — '좋은 선택'이라고 보는 기준이 뭐예요? — 오래 남는 것인지, 지금 당장 편한 것인지, 아니면 다른 뭔가인지."

**10/10/10** (답이 나오지 않을 때):
> "그 선택을 했다고 상상하면 10분 뒤 첫 감정이 뭐예요? 10년 뒤에 돌아보면요?"

### 방향형

**Pre-mortem**:
> "그 방향으로 갔다가 6개월 뒤 후회하고 있다면 이유가 뭐였을 것 같아요? — 너무 일찍 멈춘 건지, 억지로 밀어붙인 건지, 아니면 다른 뭔가인지."

**후회 최소화** (결정이 막힐 때):
> "80세에 돌아볼 때 지금 이걸 안 한 걸 후회할 것 같아요?"

### 우선순위형

**아이젠하워 2축**:
> "이것들 중에서 지금 안 하면 실제 타격이 오는 것 vs 나중에 해도 되는 것 — 어떻게 나뉘어요?"

**2차 사고** (순서가 잡히지 않을 때):
> "제일 먼저 처리하면 그 다음에 뭐가 풀려요? — 시간이 생기는 건지, 에너지가 생기는 건지, 아니면 다른 뭔가인지."

## Step 4 — 닫기 전 확인

결론 카드 출력 전 마지막 점검.

> "지금까지 보면 [방향]으로 기울어진 것 같은데 — 아직 걸리는 게 있어요? 없으면 여기서 닫을게요."

- 유저가 "없어요" 또는 짧게 동의하면 → Step 5로 이동
- 유저가 새 걱정을 꺼내면 → Step 3으로 돌아가서 처리 후 재확인

## Step 5 — 결론 카드

재확인 질문 없이 바로 출력한다.

​```
📌 결정: [선택한 것]

🎯 기준
- [핵심 기준 1 — 유저가 대화 중 쓴 언어 그대로]
- [기준 2 — 대화에서 두 번째 기준이 언급됐을 때만 추가, 없으면 생략]

⚠️ 알고 하는 것: [가장 큰 리스크 or 포기하는 것]

👣 첫 걸음: [결정 후 첫 번째 행동]

📅 날짜: YYYY-MM-DD
​```
```

- [ ] **Step 2: 파일이 올바르게 저장됐는지 확인한다**

```powershell
Get-Content "C:\Users\wwyut\.claude\plugins\결정\결정.md" -TotalCount 5
```

예상 출력:
```
---
name: 결정
description: "이거 결정 못 하겠어"가 나올 때 쓰는 스킬. 고민을 던지면 반영 → GROW 흐름 → 유형별 프레임워크 질문 → 결론 카드로 닫아준다.
---
```

---

### Task 2: 스킬 동작 검증 (3가지 유형 테스트)

**Files:**
- 없음 (새 Claude Code 세션에서 대화 테스트)

**Interfaces:**
- Consumes: Task 1에서 수정한 스킬 파일

> ⚠️ 각 테스트는 **새 Claude Code 세션**에서 진행한다. 같은 세션에서 연이어 하면 이전 대화가 영향을 준다.

- [ ] **Step 1: 방향형 고민 테스트**

새 세션에서:
```
/결정
지금 사이드 프로젝트를 계속할지, 아니면 잠깐 쉴지 모르겠어
```

체크리스트:
- [ ] 첫 응답이 질문 없이 반영으로 시작하는가? ("계속하고 싶은 마음도 있고..." 형식)
- [ ] G턴 질문에 발판 3개가 붙어 있는가?
- [ ] O턴에서 pre-mortem 또는 후회 최소화 질문이 나오는가?
- [ ] 닫기 전 확인 턴이 있는가?
- [ ] 결론 카드에 📌 / 🎯 / ⚠️ / 👣 / 📅 다섯 항목이 모두 있는가?
- [ ] 🎯 기준에 대화 중 유저가 쓴 단어가 그대로 들어갔는가?

- [ ] **Step 2: 선택형 고민 테스트**

새 세션에서:
```
/결정
다음 달 여행을 제주도로 갈지 부산으로 갈지 못 고르겠어
```

체크리스트:
- [ ] O턴에서 가정 탐색 질문이 나오는가? ("'좋은 선택'이라고 보는 기준이 뭐예요?" 형식)
- [ ] 발판이 선택지가 아닌 예시로 제시되는가? ("예를 들면 / 아니면 완전히 다른 뭔가" 포함)
- [ ] 결론 카드 ⚠️ 항목이 유저가 말하지 않아도 채워지는가?

- [ ] **Step 3: "모르겠어" 시나리오 테스트**

방향형 테스트 대화 중 두 번째 질문에:
```
모르겠어
```
로 답했을 때:

체크리스트:
- [ ] Claude가 방향을 포기하지 않고 다른 각도의 질문으로 이어가는가? (후회 최소화 등)
- [ ] 강제로 결론 카드를 내지 않고 한 번 더 탐색하는가?

---

### Task 3: 제출 사본 업데이트 및 커밋

**Files:**
- Modify: `1_mission\3조\르니(유자애)\1주차 과제 제출 폴더\결정.md`

**Interfaces:**
- Consumes: Task 1에서 작성한 스킬 파일 내용

- [ ] **Step 1: 제출 사본을 스킬 파일과 동일하게 덮어쓴다**

`1_mission\3조\르니(유자애)\1주차 과제 제출 폴더\결정.md` 를 Task 1 Step 1의 내용과 동일하게 저장한다.

- [ ] **Step 2: 변경 내용을 확인한다**

```bash
git diff "1_mission/3조/르니(유자애)/1주차 과제 제출 폴더/결정.md"
```

기존 3단계 구조(1단계 유형파악/2단계 질문/3단계 결론카드)에서 GROW 5단계 구조로 바뀐 것이 diff에 보여야 한다.

- [ ] **Step 3: 커밋한다**

```bash
git add "1_mission/3조/르니(유자애)/1주차 과제 제출 폴더/결정.md"
git add "docs/superpowers/specs/2026-07-07-결정OS-개선-design.md"
git add "docs/superpowers/plans/2026-07-07-결정OS-개선.md"
git commit -m "르니(유자애) 결정 OS v2: GROW+유형별 프레임워크+발판 패턴으로 개선"
```

- [ ] **Step 4: `/제출` 스킬로 PR을 생성한다**

```
/제출
```
