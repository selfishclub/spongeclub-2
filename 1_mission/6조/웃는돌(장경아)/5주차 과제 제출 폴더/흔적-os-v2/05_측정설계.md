# 흔적 OS v3 — 측정 설계 (게이트 지표 확정)

> **기획의 마지막 칸.** 새 기능을 제안하지 않는다. 로드맵 예산 12h는 고정이다.
> 목적 하나: **게이트 지표 6개를 확정하고, W0 코드가 바로 구현할 기록 스키마를 못 박는다.**

- 작성일: 2026-07-30 · 판정일: **2026-09-13**
- 근거: [`PRD-v3.md`](./PRD-v3.md) §9 · [`구현계획.md`](./구현계획.md) §4

---

## 0. 왜 지금 스키마를 확정하는가

**나중에 스키마를 바꾸면 그 이전 데이터가 날아간다.** 4주 측정 중에 필드를 바꾸면 게이트 판정 자체가 불가능해진다.

그리고 더 중요한 원칙 하나:

> ### 📌 계산해서 얼린다 (compute-and-freeze)
> 9/13에 원본 테이블에서 지표를 그때 계산하지 **않는다.**
> **매주 일요일에 계산해서 `weekly_metrics`에 한 행으로 얼려 넣는다.**
>
> 이유: 중간에 로직이나 스키마가 바뀌어도 **이미 얼린 주는 그대로 남는다.** 판정이 재현 가능해진다.
> 7/10의 실패는 측정을 안 한 것이었고, 그보다 나쁜 실패는 **측정했는데 나중에 해석이 달라지는 것**이다.

---

## 1. 기록 스키마 — W0에서 이대로 실행

`schema.sql`에 아래를 추가한다. **테이블 4개, 전부 작다.** 기존 `traces`·`daily_focus`는 건드리지 않는다.

```sql
-- ① 사용자 활동 (봇이 메시지를 받을 때마다 upsert)
--    지표1(생존)·지표5(무시율)의 원천
create table user_activity (
  day        date primary key,              -- KST 기준 날짜
  messages   int  not null default 0,       -- 그날 사용자가 봇에 보낸 메시지 수
  last_at    timestamptz
);

-- ② 스튜디오 진입 로그 (P1에서 딥링크 클릭 시 1행)
--    지표4의 원천
create table studio_entry_log (
  id         bigserial primary key,
  entered_at timestamptz not null default now(),
  source     text not null,                 -- 'weekly_report' | 'briefing' | 'direct'
  seed_id    text                           -- 어떤 글감에서 왔나 (없으면 null)
);

-- ③ 발행 기록 (수동 · 텔레그램 한 줄로 입력)
--    지표2의 원천 + 정지선 교정 데이터
create table publication (
  id             bigserial primary key,
  published_at   date not null,
  channel        text not null,             -- 'brunch' | 'linkedin' | 'blog' | 'other'
  draft_fidelity text not null,             -- 'as_is' | 'half' | 'rewrote'  ⭐ 핵심
  corpus_traces  int,                       -- 그때 corpus 크기 (봇이 자동 기입)
  corpus_roles   int,                       -- 걸친 역할 수 (봇이 자동 기입)
  url            text,
  noted_at       timestamptz not null default now()
);

-- ④ 주간 스냅샷 (일요일 23:00 자동 · 한 번 쓰면 절대 수정하지 않는다)
create table weekly_metrics (
  week_start        date primary key,       -- 그 주 월요일 (KST)
  -- 지표 원천
  user_active_days  int not null,           -- 지표1: 그 주 messages>0 인 날 수
  traces_new        int not null,           -- 정지선: 신규 흔적 수
  roles_touched     int not null,           -- 정지선: 걸친 역할 수
  body_traces       int not null,           -- 지표6: '몸' 역할 흔적 수
  seed_candidates   int not null,           -- 지표3: 리포트가 제안한 글감 후보 수
  studio_entries    int not null,           -- 지표4: 스튜디오 진입 횟수
  briefs_sent       int not null,           -- 지표5 분모: 아침 브리핑 발송 건수
  briefs_ignored    int not null,           -- 지표5 분자: 발송 후 24h 무반응 건수
  evening_approved  int not null,           -- 보조: 저녁 승인(숫자 답장) 일수
  computed_at       timestamptz not null default now()
);
```

**구현 위치 (W0 · 1.5h 안에 들어가는 범위)**
| 파일 | 추가할 것 |
|---|---|
| `api/telegram.js` | 인바운드 메시지마다 `user_activity` upsert **1줄** · `발행` 명령 파싱 |
| `api/cron-morning.js` | `briefs_sent` 카운트용 발송 기록 |
| 신규 `api/cron-weekly.js` | 일요일 23:00 → `weekly_metrics` 1행 삽입 + 주간 회고 문안에 지표 4줄 |

---

## 2. 게이트 지표 6개 — 정의 · 반증 · 스키마

> **경계값에 애매한 구간을 남기지 않는다.** 판정일에 해석 논쟁이 생기면 그 자체가 실패다.

### 지표 ① — 4주 연속 사용 `치명` 🤖 자동

| | |
|---|---|
| **정의** | **W3·W4·W5·W6 네 주 전부**에서 각각 `user_active_days >= 1` |
| **통과** | **4주 모두** 활동일 ≥1 |
| **🔴 실패** | **한 주라도 0일** |
| **스키마** | `weekly_metrics.user_active_days` — **주별로 이미 한 행씩 쌓이므로 스키마 변경 없음** |
| **왜 이 정의인가** | *"살아 있다"* 를 발송이 아니라 **사용자 반응**으로 정의한다. 봇은 사용자가 죽어도 계속 보낸다 |

> ### ⚠️ 2026-07-30 정의 교정 — 이름과 정의가 어긋나 있었다
> 이전 정의는 *"마지막 주(W6)에 1일 이상"* 이었다. **그러면 W3~W5에 아무것도 안 해도 통과한다.**
> **v1이 죽은 방식이 정확히 '띄엄띄엄 쓰다가 사라짐'이었는데, 그 실패를 못 잡는 정의였다.**
> → 네 주 전부에 활동을 요구하도록 조였다. 지표 이름("4주 연속 사용")과 정의가 이제 일치한다.

**판정 시 기록법:** 통과 여부만이 아니라 **주별 활동일수 4개를 그대로 적는다**(예: `3/2/1/1`). 감소 추세는 통과해도 위험 신호다.

---

### 지표 ② — 4주 안에 실제 글 1편 발행 `치명` ⭐ 🖊️ **수동**

| | |
|---|---|
| **정의** | **2026-08-10 ~ 09-13** 사이 `publication` 행이 **1개 이상** |
| **통과** | ≥ 1편 |
| **🔴 실패** | **0편** |
| **스키마** | `publication` (기록 방법은 §3) |
| **왜 이 기간인가** | P1(본체 연결) 완료가 8/9이므로, 그 다음날부터 센다. 그 이전 발행은 이 시스템의 성과가 아니다 |

> **단일 최중요 지표.** B′("내 것을 밖에 내놓을 형태로 바꾼다")가 작동했다는 유일한 증거다.

---

### ③④는 독립 지표가 아니다 — **파이프라인 2단계**

> ### ⚠️ 2026-07-30 교정
> ④(스튜디오 진입)는 ③(재료 제안)이 있어야만 가능하다. **③이 무너지면 ④는 자동으로 무너진다.**
> 이전 판정표는 셋(③④⑤)을 **동등하게 세어** ③의 실패를 두 번 계산하고 있었다.

```
③ 재료가 나온다  ──→  ④ 그걸 눌러서 들어간다
   (파이프라인 입구)      (③ 통과 시에만 판정 가능)
```

**→ 판정 규칙: ③을 먼저 본다. ③이 실패하면 ④는 `N/A`로 기록하고 판정에서 제외한다.**
합치지 않고 단계로 두는 이유: **두 실패의 진단이 완전히 다르기 때문이다.**

| 어디서 막혔나 | 진단 | 다음에 손볼 곳 |
|---|---|---|
| ③ 실패 | **입구가 막혔다** — 재료 자체가 안 나온다 | corpus 수집 · 글감 후보 선정 로직 |
| ③ 통과, ④ 실패 | **입구는 열렸는데 안 들어간다** | 리포트 문안 · 훅 · 딥링크 위치 |

---

### 지표 ③ — 주당 발행 가능 재료 `작동 · 파이프라인 1단` 🤖 자동

| | |
|---|---|
| **정의** | `seed_candidates >= 2` 인 주가 **4주(W3~W6) 중 3주 이상** |
| **통과** | ≥ 3주 |
| **🔴 실패** | ≤ 2주 |
| **스키마** | `weekly_metrics.seed_candidates` |
| **`seed_candidates` 세는 법** | 주간 리포트가 실제로 **문안에 실은** 글감 후보 수. 후보 풀 크기가 아니라 **발송된 개수** |

---

### 지표 ④ — 스튜디오 진입 `작동 · 파이프라인 2단` 🤖 자동

| | |
|---|---|
| **정의** | `studio_entries >= 1` 인 주가 **4주 중 2주 이상** |
| **통과** | ≥ 2주 |
| **🔴 실패** | ≤ 1주 |
| **⚠️ 판정 전제** | **③이 통과했을 때만 판정한다.** ③ 실패 시 → `N/A` 로 기록하고 판정에서 제외 |
| **스키마** | `weekly_metrics.studio_entries` ← `studio_entry_log` 집계 |
| **주의** | `source='weekly_report'` 만 센다. 직접 URL 진입은 리포트의 성과가 아니다 |

---

### 지표 ⑤ — 무시율 `작동` 🤖 자동

| | |
|---|---|
| **정의** | `briefs_ignored / briefs_sent` (4주 합계) |
| **"무시"의 정의** | **아침 브리핑 발송 후 24시간 안에 `user_activity.messages` 증가가 0인 날** |
| **명목 통과선** | ≤ 0.30 |
| **⭐ 실질 통과선** | **≤ 0.25** — 아래 낙관 편향 보정 |
| **🔴 실패** | **> 0.25** (실질 기준) |
| **스키마** | `weekly_metrics.briefs_sent` / `briefs_ignored` |
| **⚠️ 조치가 다르다** | 이 지표의 실패는 **게이트 실패가 아니라 즉시 조치 사유**다 → **빈도 하향**(P7). §4 별도 규칙 A |

> ### ⚠️ 이 값은 실제보다 **낙관적이다** — 프록시임을 명시한다
> `user_activity.messages`는 **봇에 보낸 모든 메시지**를 센다. 따라서 **아침 브리핑을 안 읽고 저녁에 딴 얘기만 해도 "무시 안 함"으로 집계된다.**
> **→ 실제 무시율은 이 숫자보다 높다. 방향이 정해진 편향이다(항상 과소평가).**
>
> **왜 그대로 두는가:** 아침 브리핑엔 **버튼이 없다**(P9 — 읽는 것만으로 가치가 완결). 열람 확인 수단이 원천적으로 없으므로 정확한 측정이 불가능하다. **P9를 깨면서까지 정확도를 사는 것은 이 제품의 원칙을 파는 것**이다.
>
> **보정 방법:** 편향의 방향이 일정하므로 **경계값을 낮춰 흡수한다.**
> **0.30이 아니라 0.25를 실질 경계로 읽는다.** 0.25~0.30 구간은 *"명목상 통과지만 실제로는 위험"* 으로 보고, 그 구간에 들어가면 **빈도 하향을 검토**한다(실패 처리는 아님).

---

### 지표 ⑥ — `몸` 균형 `경고` 🤖 자동

| | |
|---|---|
| **정의** | 4주(W3~W6) `body_traces` **합계** |
| **통과** | ≥ 1 |
| **🟡 경고** | **0** — 시스템이 내 삶의 축을 못 붙잡고 있다 |
| **스키마** | `weekly_metrics.body_traces` |
| **⚠️ 판정에 쓰지 않는다** | 통과/실패를 가르지 않는다. **0이면 역할 설계를 재검토**하라는 신호일 뿐 |

---

### 요약

| # | 지표 | 급 | 통과선 | 실패선 | 자동 |
|---|---|---|---|---|:---:|
| ① | 4주 연속 사용 | **치명** | **W3~W6 각 주** 활동일 ≥1 | 한 주라도 0일 | 🤖 |
| ② | 실제 글 1편 발행 | **치명** ⭐ | ≥1편 | 0편 | 🖊️ |
| ③ | 주당 발행 재료 | 작동 · **파이프 1단** | ≥3주 | ≤2주 | 🤖 |
| ④ | 스튜디오 진입 | 작동 · **파이프 2단** | ≥2주 | ≤1주 | 🤖 |
| ⑤ | 무시율 | 작동 · **별도 조치** | **≤0.25 (실질)** | >0.25 | 🤖 |
| ⑥ | `몸` 균형 | 경고 | ≥1 | 0 (경고만) | 🤖 |

**자동 5 · 수동 1.**

> **③→④는 파이프라인이다.** ③ 실패 시 ④는 `N/A`. **⑤는 명목 0.30이 아니라 실질 0.25**로 읽는다(낙관 편향 보정).

---

## 3. 수동 지표 ② 기록법 — 30초 안에 끝나야 한다

**측정이 귀찮으면 안 하게 된다.** 7/10이 그렇게 됐다. 그래서 **사용자가 치는 것은 세 단어**로 줄이고, 나머지는 봇이 채운다.

### 발행 직후 텔레그램에 한 줄

```
발행 브런치 그대로
```

**문법: `발행 <채널> <초안충실도>`**

| 채널 | 초안충실도 | → `draft_fidelity` |
|---|---|---|
| `브런치` · `링크드인` · `블로그` · `기타` | **`그대로`** — 거의 손 안 댐 | `as_is` |
| | **`절반`** — 절반쯤 고침 | `half` |
| | **`갈아엎음`** — 처음부터 다시 씀 | `rewrote` |

### 봇이 자동으로 채우는 것

`published_at`(오늘) · `corpus_traces`(현재 흔적 수) · `corpus_roles`(걸친 역할 수) · `noted_at`

### 봇 응답

```
✅ 기록했어요.
   브런치 · 초안 그대로 · 흔적 23개 / 역할 4개
   URL 있으면 이어서 보내주세요 (안 보내도 돼요)
```

> `url`은 **선택**이다. 필수로 만들면 그 순간 30초를 넘고, 안 하게 된다.

### ⭐ `draft_fidelity`가 왜 핵심인가

**이 한 필드가 P1의 성패를 판정한다.**

| 값 | 의미 |
|---|---|
| `as_is` · `half` | ✅ 출력이 **완제품**이었다. B′가 작동했다 |
| `rewrote` | 🔴 **"완제품"이 거짓이다.** 지표 ②는 통과해도 **P1은 실패**다 — 초안이 백지와 다를 바 없었다는 뜻 |

`corpus_traces` / `corpus_roles`는 [`구현계획.md`](./구현계획.md) §2의 정지선(**흔적 8개 · 역할 3개**, n=1 관측 1회)을 교정할 유일한 데이터다.

---

## 4. 9/13 게이트 판정표

> **판정 당일에 해석 논쟁이 생기지 않게 미리 못 박는다.** 아래 표를 그대로 읽고 끝낸다.

### 판정 순서

**1단계 — 치명 지표.** ①②를 본다. 하나라도 실패면 아래를 볼 필요가 없다.
**2단계 — 파이프라인.** ③을 먼저 보고, **③이 통과했을 때만** ④를 본다. (③ 실패 → ④는 `N/A`)
**3단계 — ⑤ 무시율은 판정 칸에서 빼고** 별도 규칙 A로 처리한다.
**4단계 — ⑥은 판정에 넣지 않고 경고로만 읽는다.**

> ⚠️ **⑤를 판정 칸에서 뺀 이유:** 무시율은 제품 가설의 실패가 아니라 **개입 총량의 실패**다. 조치(빈도 하향)가 다르므로, 통과/실패 계산에 섞으면 잘못된 결론이 나온다.

| ① 생존 | ② 발행 | ③ 재료 | ④ 진입 | 판정 | 진단 · 무엇을 하나 |
|:---:|:---:|:---:|:---:|---|---|
| ✅ | ✅ | ✅ | ✅ | 🟢 **통과** | P3 이후 진행. 확장군 인터뷰(P6) 착수 가능 |
| ✅ | ✅ | ✅ | ❌ | 🟡 **조건부 통과** | **입구는 열렸는데 안 들어간다.** 리포트 문안·훅·딥링크 위치만 손본다. **새 기능 금지 · 4주 더 관찰 · 10/11 재판정** |
| ✅ | ✅ | ❌ | `N/A` | 🔴 **실패 — 파이프라인 입구** | **재료 자체가 안 나온다.** corpus 수집·글감 후보 선정 로직부터 재검. P3 이후 전부 보류 |
| ✅ | ❌ | any | any | 🔴 **실패 — 최중요 지표** | **B′ 가설 자체를 재검토한다.** 확장군 인터뷰 금지. `01_JTBD검증.md`로 돌아간다 |
| ❌ | any | any | any | 🔴 **실패 — v1 재현** | 축이 아니라 **습관**의 문제. 전부 접고 원인부터 규명 |

> 📌 **③❌ 인데 ②✅ 인 경우** (재료 제안은 없었는데 발행은 했다) → **3행(🔴 실패)으로 판정한다.**
> 그 발행은 이 시스템 때문이 아니다. 별도 규칙 B와 함께 읽을 것.

### 별도 규칙 3개

**A. ⑤ 무시율 — 판정 칸 밖에서 별도로 읽는다**

| 값 | 읽는 법 | 조치 |
|---|---|---|
| ≤ 0.25 | 🟢 실질 통과 | 없음 |
| 0.25 ~ 0.30 | 🟡 **명목 통과 · 실질 위험** (낙관 편향 구간) | **빈도 하향 검토** |
| > 0.30 | 🔴 명목까지 실패 | **즉시 빈도 하향** (P7 — 하루 2터치 → 1터치) · 4주 재관찰 |

→ 어느 경우도 **게이트 실패로 치지 않는다.**
근거: 무시율은 제품 가설의 실패가 아니라 **개입 총량의 실패**다. 그리고 이 값은 프록시라 **실제보다 낙관적**이므로(§2 지표⑤), 실패 판정의 근거로 쓰기엔 정밀도가 부족하다.

**B. ② 통과했는데 `draft_fidelity = 'rewrote'` 인 경우**
→ 게이트는 통과하되 **P1을 실패로 기록**한다. 다음 4주는 **새 기능 없이 글감 제안 품질만** 손본다.
근거: 발행은 됐지만 이 시스템 때문이 아니다.

**C. ⑥ `몸` = 0 인 경우**
→ 판정과 무관. 다만 **P5 역할 재편의 최우선 항목**으로 올린다.
근거: 1주차에 본인이 중요하다고 지목한 축이 두 번째로 떨어진 것이다.

### 판정일에 할 일 (10분)

1. `select * from weekly_metrics order by week_start;` → 4행 확인
2. `select * from publication where published_at between '2026-08-10' and '2026-09-13';`
3. 위 표에서 해당 행을 찾아 **그대로 읽는다**
4. 결과와 판정을 이 문서 §5에 기록

---

## 5. 판정 기록란 (9/13에 작성)

- 판정일:
- ① 생존 — **주별 활동일수** W3:___ / W4:___ / W5:___ / W6:___ → **4주 모두 ≥1인가** ✅/🔴
  - (통과해도 **감소 추세면 위험 신호**로 기록)
- ② 발행: ___편 · `draft_fidelity`: ___ → ✅/🔴
- ③ 재료 (2개 이상인 주): ___/4주 → ✅/🔴
- ④ 진입 (1회 이상인 주): ___/4주 → ✅/🔴 / **`N/A`(③ 실패 시)**
- ⑤ 무시율: ___ → 실질 경계 **0.25** 기준 ✅/🔴 · **판정 칸 아님 (별도 규칙 A)**
  - (0.25~0.30 구간이면 *"명목 통과·실질 위험"* → 빈도 하향 검토)
- ⑥ `몸` 합계: ___ → 경고 여부
- **판정:**
- **다음 4주에 할 일:**

---

## 6. Act / Watch 정리

### Act — W0에서 즉시 (측정 자동화 1.5h 안)

| # | 무엇 | 구체적 첫 단계 |
|---|---|---|
| 1 | 테이블 4개 생성 | §1 SQL을 Supabase SQL Editor에 붙여넣고 Run |
| 2 | `user_activity` upsert | `api/telegram.js` 인바운드 핸들러 첫 줄에 1줄 추가 |
| 3 | `발행` 명령 파싱 | 같은 파일. 3토큰 파싱 + `publication` insert |
| 4 | 주간 스냅샷 cron | `api/cron-weekly.js` 신규 · 일요일 23:00 · `weekly_metrics` 1행 + 회고 문안에 지표 4줄 |

### Watch — 판정 전까지 건드리지 않음

| # | 신호 | 언제 본다 |
|---|---|---|
| 1 | 주당 흔적 10개 미만 (정지선 미달) | 매주 자동. 미달 시 `답장이 곧 캡처`를 게이트 앞으로 되돌림 |
| 2 | Notion 개인 요금제 커넥터 개방 | **10/1** 이후 분기별 (구현계획 Q1) |
| 3 | 확장군 가설 (②③ 포지셔닝) | **게이트 통과 후 P6.** 그 전엔 아무것도 하지 않는다 |
